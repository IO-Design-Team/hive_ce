name: Publish hive_ce_flutter to pub.dev

on:
    push:
        tags:
            - "hive_ce_flutter-[0-9]+.[0-9]+.[0-9]+*"

jobs:
    publish:
        permissions:
            id-token: write
        uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
        with:
            working-directory: hive_flutter
