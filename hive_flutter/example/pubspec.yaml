name: example
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  rexios_lints: ^7.0.0

flutter:
  uses-material-design: true
