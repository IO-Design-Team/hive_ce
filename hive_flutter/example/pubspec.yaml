name: example
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  hive_ce_flutter:
    path: ../../hive_flutter

dev_dependencies:
  rexios_lints: ^17.0.2
  build_runner: ^2.5.4
  hive_ce_generator:
    path: ../../hive_generator

dependency_overrides:
  hive_ce:
    path: ../../hive

flutter:
  uses-material-design: true
