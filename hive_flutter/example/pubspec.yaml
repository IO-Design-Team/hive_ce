name: example
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  hive_ce_flutter:
    path: ../../hive_flutter

dev_dependencies:
  rexios_lints: ^7.0.0
  build_runner: ^2.4.0
  hive_ce_generator:
    path: ../../hive_generator

# dependency_overrides:
#   hive_ce:
#     path: ../../hive

flutter:
  uses-material-design: true
