name: hive_ce_flutter
description: Extension for Hive. Makes it easier to use Hive in Flutter apps.
version: 3.0.0
homepage: https://github.com/IO-Design-Team/hive_ce/tree/main/hive_flutter

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  material_ui: ^1.0.0

  hive_ce: ^2.16.0
  path_provider: ^2.0.10
  path: ^1.8.2

dev_dependencies:
  test: ^1.21.1
  rexios_lints: ^19.0.0
  mockito: ^5.4.1
  build_runner: ^2.5.4
